#lang racket
(define (sorted-texted-tree? string)
  ;тебе понадобятся проги на вставку в самые дальние ветки справа и слева вставку справа и слева
  (define (texted-tree->tree string)
    (define (iter tail tree)
      (if (null? tail)
          )))
  
  (define (sorted-tree? tree)
    )
  
  (sorted-tree? (texted-tree->tree string))
  )